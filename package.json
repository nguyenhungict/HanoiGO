{
  "name": "hanoigo",
  "scripts": {
    "dev": "concurrently \"npm run dev:actions\" \"npm run dev:client\"",
    "dev:actions": "cd actions && npm run start:dev",
    "dev:client": "cd client && npm run dev",
    "install:all": "cd actions && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "latest"
  },
  "dependencies": {
    "pdf-parse": "^2.4.5"
  }
}
